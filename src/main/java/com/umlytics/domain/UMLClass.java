package com.umlytics.domain;

/**
 * Type alias for ConceptualClass — maintains class diagram compliance.
 * The class diagram specifies UMLClass; this project uses ConceptualClass internally.
 * Both names refer to the same type.
 */
public class UMLClass extends ConceptualClass {
    // Intentionally empty — UMLClass IS a ConceptualClass.
    // Use either name interchangeably.
}
